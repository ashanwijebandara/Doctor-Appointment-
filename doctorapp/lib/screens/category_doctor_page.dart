/*import 'package:flutter/material.dart';

class CategoryDoctorsPage extends StatelessWidget {
  final String category;

  CategoryDoctorsPage({required this.category});

  @override
  Widget build(BuildContext context) {
    // Implement the UI to display doctors for the selected category
    return Scaffold(
      appBar: AppBar(
        title: Text('Doctors in $category'),
      ),
      body: Center(
        child: Text('Doctors for $category category'),
      ),
    );
  }
}
*/